# 验证记录

## v0.2 媒体胶囊（2026-09-22）

平台：本机 Windows x64，.NET SDK 10.0.301。

| 检查 | 结果 | 实际证明的范围 |
| --- | --- | --- |
| Release 编译 | PASS，0 警告、0 错误 | WinRT Windows SDK 引用、Core、WPF 与测试均可编译 |
| Core 行为测试 | PASS，13/13 | 原有行为，以及 LRC/增强 LRC/offset/回退跳转、暂停倍速、seek 边界、来源选择、命令能力与拒绝、过期异步结果、销毁 |
| WPF / Win32 回归 | PASS | 原有图标与窗口测试；媒体卡片渲染、实际进度值、控制禁用、空状态、歌词清理、重复歌词前进/回退预告 |
| 真实 GSMTC 读取探针 | PASS，发现 0 个现有会话 | 可访问本机 Windows 媒体服务；未据此宣称支持任何第三方播放器 |
| 原生 SMTC/GSMTC 集成 | PASS | 自建无声 SMTC 会话被实际后端发现；元数据和能力读取、重复枚举身份稳定、暂停事件及 65 秒 seek 事件实际送达 |
| Windows x64 发布与启动 | PASS | 依赖 Desktop Runtime 的发布目录可运行；可见 FloatDock 窗口建立，结束后关闭测试进程 |
| 第三方播放器、歌词匹配率 | 未验收 | 尚未逐个验证 Spotify、网易云、QQ 音乐、浏览器等；在线接口错误有回退，但未测歌词覆盖率 |
| 动画手感、实际桌面交互、DPI | 未验收 | 合成界面渲染和专用测试会话不代替用户体验验证 |

媒体核心测试先在缺失实现时失败，实现后通过。审查发现连续相同歌词会令下一句预告不刷新；新增前进/回退用例，先复现失败，再通过记录歌词行索引修复。独立代码审查未发现其他明确阻塞问题。

`--media-integration` 在交互式桌面显式执行；使用测试窗口自己的 SMTC，不播放声音，仅向随机唯一标题匹配的测试会话发送命令，最后禁用会话并清理元数据。默认 CI 不依赖系统媒体服务。

`docs/assets` 两张 PNG 为实际 WPF 控件配合合成元数据/歌词渲染。已查看布局与文字显示，不含用户媒体信息，也不冒充真实播放器播放截图。

媒体人工验收待办：真实播放器切歌、暂停/恢复、拖动进度、多来源切换、会话关闭后恢复空状态；导入本地歌词及开启/关闭在线匹配；缩放与全屏时卡片行为。

## v0.1 首版（历史记录）

日期：2026-09-22。平台：本机 Windows x64，.NET SDK 10.0.301。

| 检查 | 结果 | 实际证明的范围 |
| --- | --- | --- |
| Release 编译 | PASS，0 警告、0 错误 | Core、WPF 应用及测试项目可以编译 |
| Core 行为测试 | PASS，8/8 | 固定与运行应用合并、大小写去重、固定排序、无路径窗口、窗口循环、动画目标、宽度限制、空标题回退、全屏规则 |
| WPF / Win32 回归 | PASS | 64 DIP 图标悬停+弹跳不越界、实际光栅像素存在、减少动画立即取消缩放、所属对话框前台归属 |
| Windows x64 发布 | PASS | 产生依赖 .NET Desktop Runtime 的可执行目录 |
| 发布程序启动检查 | PASS | 进程响应且创建可见 FloatDock 窗口；检查后已关闭测试进程 |
| 桌面交互、动画手感 | 未验收 | 需真实鼠标操作及作者体验 |
| 混合 DPI、多屏、休眠唤醒 | 未验收 | 需对应物理环境测试 |

测试先运行于空实现，6 项均失败；实现后 6 项通过。没有将检查源码文本作为行为测试。

独立审查后新增空标题回退、最大化/全屏规则及所属对话框归属测试，分别先复现失败，再修复到通过。最终 Core 为 8/8，Windows 测试程序的两个检查组均通过。

WPF 回归使用屏幕外的专用测试窗口驱动真实动画时钟，发现最大图标组合动画顶部越界约 12 DIP。增加上方空间、保持底部位置后测试通过。这是自动化布局与渲染检查，不是桌面截图或人工观感验收。

启动脚本最初使用 `Process.MainWindowHandle`，漏掉了 `ShowInTaskbar=false` 的 WPF 窗口。通过仅枚举测试进程窗口，确认真实 FloatDock 窗口可见且带隐藏属主后，修正脚本以测试进程 ID、标题和可见性识别窗口。此修复没有放宽到“只要进程活着就通过”。

## 人工验收清单

- 开启程序时不会抢走当前应用的键盘焦点。
- 鼠标左右划过图标时，无抖动、裁切或明显卡顿；移出后平滑复位。
- 点击已打开、最小化、多个窗口的同一程序，验证正确切换；退出后无残留。
- 添加、取消固定，切换底板和尺寸，退出重开验证设置。
- 任务栏自动隐藏开启/关闭时，Dock 距离屏幕底部合理。
- 主屏全屏程序前台时 Dock 隐藏，退出全屏后重新出现。
- 图标过多时可以滚动到首尾；125% / 150% / 200% 缩放下无裁切。

首版不以此记录宣称完成体验验收或达到稳定版质量。
